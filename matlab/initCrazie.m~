% function that I use to setup my environment
function initCrazie()
    % store the current directory
    cwd = pwd;
    cd ~/drake-distro/drake;
    
    % add drak
    addpath_drake;
    cd(cwd);
end